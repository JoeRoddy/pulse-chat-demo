import { SignupDialog } from '@/components/features/signup/signup-dialog';
import { NextPage } from 'next';

const HomeScreen: NextPage = ({}) => {
  return <SignupDialog />;
};

export default HomeScreen;
